<%= form_for @banner, :html => { :multipart => true } do |f| %>
  <% if @banner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@banner.errors.count, "error") %> prohibited this banner from being saved:</h2>

      <ul>
      <% @banner.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  
</div>
  <div class="clearfix">
    <%= f.label :title, "Titulo" %>
    <div class="input">
      <%= f.text_field :title, :class => "xxlarge" %>
    </div>    
  </div>
  <div class="clearfix">
    <%= f.label :subtitle, "Subtitlo" %>
    <div class="input">
      <%= f.text_field :subtitle, :class => "xxlarge" %>
    </div>    
  </div>
    <div class="clearfix">
      <%= f.label :image, "Imagen" %>
      <div class="input">
        <%= f.file_field :image ,:class => "xlarge input-file" %>
      </div>        
    </div>
  <div class="actions">
    <%= f.submit :class => "btn primary" %>
    <a href="/banners" class="btn">Cancelar</a>
  </div>
<% end %>
