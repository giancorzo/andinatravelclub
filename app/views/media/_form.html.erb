<%= form_for @medium, :html => { :class => "form-horizontal",  :multipart => true } do |f| %>
  <% if @medium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medium.errors.count, "error") %> prohibited this medium from being saved:</h2>

      <ul>
      <% @medium.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :content, "Image:",:class => "control-label" %>
    <div class="controls">
      <%= f.file_field :content ,:class => "input-xlarge input-file" %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :description, "Descripción:",:class => "control-label" %>
    <div class="controls">
      <%= f.text_area :description, :rows => 3, :style => "width: 360px;" %>
    </div>      
  </div>
  <%= f.submit :class => "btn-primary", :style => "margin-left: 160px;"%> 
<% end %>
